services:
  db:
    image: postgres:14.2
    environment:
      POSTGRES_DB: testdb
      POSTGRES_USER: testuser
      POSTGRES_PASSWORD: testpass
    volumes:
      - db-data:/var/lib/postgresql_container/data
  pgadmin4:
    image: dpage/pgadmin4:6.9
    depends_on:
      - db
    environment:
      PGADMIN_DEFAULT_EMAIL: awkward2017@yahoo.co.jp
      PGADMIN_DEFAULT_PASSWORD: samplepass
    ports:
      - "8080:80"
    volumes:
      - pgadmin4-data:/var/lib/pgadmin_container
volumes:
  db-data:
  pgadmin4-data: